package com.eat_healthy.tiffin.viewHolder

import androidx.recyclerview.widget.RecyclerView
import com.eat_healthy.tiffin.databinding.AdapterSummarySelectedMealBinding
import com.eat_healthy.tiffin.models.Header
import com.eat_healthy.tiffin.models.SummarySelectedMeal

class SummarySelectedMealViewHolder (val itemBinding: AdapterSummarySelectedMealBinding) : RecyclerView.ViewHolder(itemBinding.root) {
    fun bind(item: SummarySelectedMeal){
    }
}
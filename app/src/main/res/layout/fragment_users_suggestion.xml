<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:too="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools">

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/windowBackground"
        >

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:fontFamily="@font/roboto_regular"
            android:layout_marginStart="15dp"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="20dp"
            tool:text="Mealy Feedback Form"
            />
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvSubHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textColor="@color/text_color"
            android:fontFamily="@font/roboto_regular"
            android:layout_below="@+id/tvHeader"
            android:layout_marginStart="15dp"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="7dp"
            android:layout_marginBottom="20dp"
            android:visibility="gone"
            android:text="Please provode suggestion/feedback for better experinece"
            />
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_parent"
            android:layout_width="match_parent"
            android:layout_below="@+id/tvSubHeader"
            android:layout_height="match_parent"
            android:layout_marginTop="8dp"
            android:layout_above="@+id/tvSubmitFeedback"
            android:layout_marginHorizontal="15dp"
            android:paddingHorizontal="3dp"
            android:layout_marginBottom="20dp"
            android:background="@drawable/rounded_corner"
            android:backgroundTint="@color/white"
            android:visibility="visible"
            />
        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvSubmitFeedback"
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:text="Submit Feedback"
            android:gravity="center"
            android:textStyle="bold"
            android:layout_marginHorizontal="20dp"
            android:paddingHorizontal="30dp"
            android:textColor="@color/white"
            android:background="@drawable/little_rounded_corner"
            android:backgroundTint="@color/colorPrimary"
            android:layout_alignParentBottom="true"
            android:layout_marginBottom="10dp"
            android:visibility="gone"
            android:textSize="18sp"
            android:paddingVertical="8dp"
            />
    </RelativeLayout>
</layout>